[project]
name = "gstack-port-for-codex"
version = "0.1.0"
description = "Codex-native ports of gstack skills"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Praneet Koppula" }
]


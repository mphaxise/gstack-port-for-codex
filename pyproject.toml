[project]
name = "gstack-port-for-codex"
version = "0.1.0"
description = "Codex-native ports of the full GStack surface plus a full-surface GBrain workflow port"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Praneet Koppula" }
]

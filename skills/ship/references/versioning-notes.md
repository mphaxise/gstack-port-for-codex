# Versioning Notes

If the repo already uses `VERSION` or `CHANGELOG.md`, keep the update policy simple:

- small fixes or narrow features: patch-level bump
- larger user-facing milestones: minor-level bump
- breaking or major repositioning work: major-level bump

If no versioning files exist, do not add them during a ship pass unless the user explicitly asks.

